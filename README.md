# Ethical-Hacking-Toolkit

***Установка:***
1. Windows
   Скачать .zip репозитория,
   ```
   cd server && pip install -r requirements.txt && uvicorn main:app --reload --port 8000 && python -c "import webbrowser; webbrowser.open('https://121.0.0.1:8000')"
   ```
